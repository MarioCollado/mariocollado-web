<section
  id="contact"
  class="h-screen flex flex-col justify-center items-center bg-primary-light text-gray-800 relative snap-start"
>
  <!-- Contenedor principal (estilo idéntico a Tech) -->
  <div
    class="text-center max-w-2xl w-full p-8 bg-secondary-cyan rounded-none border-4 border-black space-y-8"
  >
    <h1 class="text-6xl font-bold mb-4 font-druk">CONTACTO</h1>
    <p class="text-lg">
      Escríbeme y me pondré en contacto contigo lo antes posible.
    </p>

    <!-- Formulario con el mismo estilo que las cards de Tech -->
    <form class="space-y-6">
      <div class="space-y-2 text-left">
        <label class="font-bold">Nombre</label>
        <input
          type="text"
          class="w-full p-4 bg-white border-2 border-black focus:border-secondary-pink focus:shadow-[0px_0px_7px_2px_black] transition-all duration-200"
          placeholder="Tu nombre"
        />
      </div>

      <div class="space-y-2 text-left">
        <label class="font-bold">Email</label>
        <input
          type="email"
          class="w-full p-4 bg-white border-2 border-black focus:border-secondary-pink focus:shadow-[0px_0px_7px_2px_black] transition-all duration-200"
          placeholder="tu@email.com"
        />
      </div>

      <div class="space-y-2 text-left">
        <label class="font-bold">Mensaje</label>
        <textarea
          rows="4"
          class="w-full p-4 bg-white border-2 border-black focus:border-secondary-pink focus:shadow-[0px_0px_7px_2px_black] transition-all duration-200"
          placeholder="Tu mensaje..."></textarea>
      </div>

      <button
        type="submit"
        class="w-full bg-secondary-pink text-white px-8 py-4 rounded-none border-4 border-black hover:shadow-[0px_0px_7px_2px_black] transition-all duration-200 font-bold"
      >
        ENVIAR
      </button>
    </form>
  </div>
</section>
