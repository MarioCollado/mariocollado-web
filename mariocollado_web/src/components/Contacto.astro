---
// src/components/Contacto.astro
---

<section
  id="contact"
  class="h-screen flex flex-col justify-center items-center bg-secondary-red text-white relative space-y-8"
>
  <!-- Flecha para volver a la sección anterior -->
  <a
    href="#tech"
    class="absolute top-8 left-1/2 transform -translate-x-1/2 cursor-pointer opacity-0 hover:opacity-100 transition-opacity duration-300"
  >
    <img
      src="/arrow-bottom-white.svg"
      alt="Flecha arriba"
      class="w-10 h-10 rotate-180"
    />
  </a>

  <div
    class="text-center max-w-md w-full p-6 bg-white/10 rounded-2xl shadow-lg"
  >
    <h1 class="text-4xl mb-4">CONTACTO</h1>
    <p class="mb-6">Si deseas contactarme, completa el siguiente formulario:</p>

    <form action="#" method="POST" class="flex flex-col space-y-4">
      <label for="name" class="text-left">Nombre</label>
      <input
        type="text"
        id="name"
        name="name"
        required
        class="p-3 rounded-md text-black"
        placeholder="Tu nombre..."
      />

      <label for="email" class="text-left">Correo Electrónico</label>
      <input
        type="email"
        id="email"
        name="email"
        required
        class="p-3 rounded-md text-black"
        placeholder="Tu correo..."
      />

      <label for="message" class="text-left">Mensaje</label>
      <textarea
        id="message"
        name="message"
        rows="4"
        required
        class="p-3 rounded-md text-black"
        placeholder="Escribe tu mensaje aquí..."></textarea>

      <button
        type="submit"
        class="bg-primary-dark text-white py-3 rounded-md font-bold hover:bg-primary-light transition duration-300"
      >
        Enviar
      </button>
    </form>
  </div>
</section>
